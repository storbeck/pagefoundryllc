{
  "buildCommand": "npm run vercel-build",
  "outputDirectory": "dist",
  "functions": {
    "api/ssr.js": {
      "includeFiles": "dist/**"
    }
  },
  "routes": [
    { "src": "/api/ssr", "dest": "/api/ssr" },
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/api/ssr" }
  ]
}

{
  "name": "pagefoundry-landing",
  "version": "1.0.0",
  "description": "Landing page for PageFoundry LLC - Vue.js SSR",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "dev": "node server",
    "build": "vite build && vite build --ssr src/entry-server.js --outDir dist/server --emptyOutDir false",
    "vercel-build": "npm run build",
    "serve": "node server"
  },
  "dependencies": {
    "@vue/server-renderer": "^3.3.4",
    "express": "^4.18.2",
    "swiper": "^10.3.1",
    "vue": "^3.3.4"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^4.4.0",
    "vite": "^4.4.0"
  }
}
